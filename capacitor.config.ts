import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'hybrid.chatbox.botbanhang.vn',
  appName: 'chatbox',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
